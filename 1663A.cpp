#include <bits/stdc++.h>
using namespace std;

int main(){
	cout<<"red panda";

	return 0;
}

int r = (abs(sum)/2) + abs(sum)%2; 
            if (r%2==1){
                product = -product; 
            }
            if (product == -1){
                cout << r + 1 << endl; 
            }
            else{
                cout<<r<<endl; 
            }
    